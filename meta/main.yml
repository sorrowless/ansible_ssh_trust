galaxy_info:
  author: Stan Bogatkin
  description: Ansible role to ensure SSH trust between nodes
  company: Personal project
  role_name: ssh_trust

  license: Apache

  min_ansible_version: 2.4

  platforms:
  - name: Ubuntu
    versions:
    - xenial
    - bionic
    - focal

  galaxy_tags: [ssh, trust]

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
