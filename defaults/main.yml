---
# defaults file for ssh-trust
ssh_trust: []
ssh_trust_defaults:
  - master: false
    group: default
    # User with this name on masters will create fingerprints for slaves
    # by reaching them.
    trust_user: backuper
